<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kz.iitu.itse1903.birlikbai.newsapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Auth.RegisterActivity"/>
        <activity android:name=".Auth.LoginActivity"/>

        <activity android:name=".Auth.MainAuthActivity"
            android:theme="@style/AppTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".App.activities.MainActivity"
            android:theme="@style/AppTheme"
            android:exported="true"></activity>

        <activity android:name=".App.activities.NewsDetail"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="true"></activity>

        <activity android:name=".App.activities.GeneralNews"
            android:theme="@style/AppTheme"
            android:exported="true"></activity>

        <activity android:name=".App.activities.Favorites"
            android:theme="@style/AppTheme"
            android:exported="true"></activity>


    </application>

</manifest>

